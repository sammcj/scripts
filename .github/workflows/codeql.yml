#### THIS FILE IS MANAGED BY AN AUTOMATED WORKFLOW ####

name: "CodeQL"

on:
  schedule:
    - cron: "25 17 * * 3"

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-22.04
    permissions:
      actions: read
      contents: read
      security-events: write
      pull-requests: write
      checks: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3

      - name: Initialize CodeQL
        uses: github/codeql-action/init@ffd3158cb9024ebd018dbf20756f28befbd168c7 # v2

      # Autobuild attempts to build any compiled languages  (C/C++, C#, or Java).
      # If this step fails, then you should remove it and run the build manually (see below)
      - name: Autobuild
        uses: github/codeql-action/autobuild@ffd3158cb9024ebd018dbf20756f28befbd168c7 # v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@ffd3158cb9024ebd018dbf20756f28befbd168c7 # v2
